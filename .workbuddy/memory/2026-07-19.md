# 2026-07-19

## 趣玩小工具（原生微信小程序）完成开发

在 `C:\Users\zhang\Desktop\YHdesign` 根目录开发了纯原生微信小程序「趣玩小工具」，未新建额外文件夹。

### 已实现
- 全局：app.json（9 页面 + 纯文字 TabBar 首页/我的 + style:v2）、app.wxss（浅蓝治愈设计系统）、project.config.json（appid=touristappid）、sitemap.json
- utils：util.js（存储封装/FNV-1a+mulberry32 确定性随机/星座生肖推算）、fortuneData.js、constellationData.js（元素契合）、zodiacData.js（六合/三合/相冲）
- 7 个功能页 + 首页 + 我的页，每页 .js/.json/.wxml/.wxss 四件套
- 幸运转盘：canvas 2d 绘制 + CSS transform 旋转 ~4s，先定结果再反向算旋转角，保证落点正确；支持增删（上限12）、4 模板
- 备忘录/倒数日记：底部弹层增删改查、筛选搜索、本地存储
- 敲木鱼：合成音效 audio/woodfish.wav（Node 生成 240Hz 中空「咚」声，22KB）+ 震动 + 缩放 + 功德飘字
- 运势/匹配：同一输入结果确定性固定

### 校验
- node --check 全部 14 个 JS 通过；12 个 JSON 合法；9 个页面四件套齐全
- 主包总大小 ~88KB（远低于 2MB 上限）

### 注意事项
- 木鱼音效引用路径 `/audio/woodfish.wav`（包内资源，相对根目录）；若个别基础库不播放，已 try/catch 兜底，不影响交互
- 生肖选择器用完整日期 picker（兼容性好），仅展示年份
- 备忘录提醒仅记录日期，不推送
- 用户强调：UI 严格、不错位、浅蓝治愈风、最佳实践

## 第二轮：4 项 bug/优化（同日追加）
用户反馈后修改：
1. **我的页改个人信息**：about 页重写为可编辑表单（昵称/性别/生日），生日自动推算星座+生肖，存 `qw_profile`；运势与每日抽签据此生成。
2. **音效/动效补强**：新增 `utils/sound.js` 统一播放包内 wav（全局静音开关 `qw_muted`，obeyMuteSwitch=false 保证静音模式也响）；用 Node 重新合成 3 个音效：woodfish(木质「咚」190/330Hz+瞬态)、draw(660→880 钟铃)、result(784→1046)。首页/运势/转盘/抽签均接入音效与过渡动画。
3. **木鱼优化**：移除 `wx.vibrateShort`、改用 sound.play('woodfish')；动画改为弹性回弹(cubic-bezier 过冲)+涟漪环，去掉生硬缩放。
4. **新增每日抽签页**：`pages/draw`（已加入 app.json 与首页宫格，现共 10 页面）。签池 12 支，点击后洗签抖动动画(~1.4s 快速切换)落定，按「日期+生日」确定性出签，存当日已抽态。
- 校验：16 个 JS、14 个 JSON 全过；10 页面四件套齐全；总大小 ~210KB（含 wav，仍远低于 2MB）。

## 第三轮：转盘与木鱼再优化（同日追加）
用户反馈后修改：
1. **转盘重构**：由 canvas 2d 改为 CSS `conic-gradient` + 绝对定位标签（参考 desktop/test/小决定），解决进入卡顿、绘制时机错位、旋转落点不准问题；现在文字始终水平可读，指针与扇区严格对齐。
2. **转盘音效**：旋转时加入 `tick.wav` 滴答循环（频率由快到慢模拟减速），落定后播放 `result.wav` 提示音。
3. **自定义模板**：快捷模板区新增「＋ 新增模板」，用户可输入模板名称和选项（换行/逗号分隔）保存为自定义模板，立即应用；自定义模板可删除，存 `qw_wheel_custom_templates`。
4. **电子木鱼按参考图重做**：黑底白鱼（`/images/woodfish.svg`），去除震动和花哨涟漪，改为简洁的点击缩放反馈；音效重新合成为更干脆的木块声；功德 +1 从鱼顶部飘出。
- 新增文件：`utils/wheel.js`、`images/woodfish.svg`；更新 `utils/sound.js`（增加 tick 循环）、`audio/tick.wav`、`audio/result.wav`、`audio/woodfish.wav`。
- 校验：17 个 JS、14 个 JSON 全过；10 页面四件套齐全；总大小 ~187KB。

## 第四轮：全站 UI 精致化重做（同日追加）
用户要求「每个页面都浏览一遍，用更精美的 UI 重新设计，可以参考好设计 / 用组件库，并且每次写完页面能自己看看好不好」。
采纳 frontend-dev 设计技能（去 emoji、统一令牌、触感反馈、入场 stagger、空/加载/错误态），在 WeChat WXSS 语境下落地：

1. **建立全局设计系统 v2（app.wxss）**：CSS 变量令牌（主色/墨色/阴影/圆角）、卡片/按钮/输入框/弹层/星级通用类、入场动画（fadeUp/pop + 可用 animation-delay 做 stagger）、**纯内联 SVG 图标集（21 个 .ic-* 类，无 emoji）**，用 `filter:brightness(0) invert(1)` 做 `.ic-w` 白色变体。
2. **逐页重做**：
   - 首页：hero 问候语 + 8 宫格（内联 SVG 图标 tile + stagger 入场）
   - 我的：渐变资料头（SVG 头像）+ 表单 + 隐私卡片
   - 幸运转盘：修复「进入空白/延迟」——onLoad 前置默认模板与 conic 渐变，转盘一进即显；标签改为径向排布（旋转后观感一致）；中心 hub 可点；结果弹层加色块徽标
   - 今日运势：新增「综合」分数环（conic-gradient）+ 已绑定生日徽标
   - 星座/生肖：VS 圆徽 + 圆形头像 + 分数环（按等级上色）+ 点评卡
   - 备忘录：分类左侧色条 + 搜索图标 + SVG 删除按钮 + SVG 空状态
   - 倒数日记：大号「天」数卡片 + SVG 删除/空状态
   - 敲木鱼（重点）：重绘 `/images/woodfish.svg` 为真正木鱼剪影 + 柔光 + 金色「功德+1」飘字 + SVG 图标按钮（去掉 emoji）
   - 每日抽签：签文做成「印章」seal 卡 + 洗签呼吸动画 + 已抽徽标
3. **自审机制**：生成 `preview.html`（10 个 360px 手机框，镜像新设计系统），可在浏览器面板逐屏查看自评。
4. **校验**：17 个 JS、14 个 JSON 全过；10 页面四件套齐全；总大小 ~237KB（含 wav+svg，仍远低于 2MB）。

### 关键约定（延续）
- 用户强调：UI 严格、不错位、浅蓝治愈风、最佳实践；在本目录开发不新建额外文件夹。
- 转盘旋转角度算法（`utils/wheel.js` calculateRotation）正确，指针严格对准选中扇区中心（先前 canvas 版才错位）。

## 第五轮：5 项细节修正（同日追加）
用户反馈后修改：
1. **木鱼换样子**：重绘 `/images/woodfish.svg` 为更精致可爱的木鱼（圆润鱼身 + 尾部鳍 + 敲击裂缝 + 双眼带高光 + 柔光），仍是黑底白鱼风格。
2. **输入对话框完整可见**：底部弹层（备忘录/倒数日记/转盘模板）加键盘高度跟踪（`bindfocus`/`bindblur` 设 `kbHeight`，`.sheet` 用 `bottom: {{kbHeight}}px` 随键盘上移）；`.sheet` 加 `max-height:90vh` 且 body 用 `scroll-view` 可滚动；确保输入框不被键盘遮挡、对话框完整可见。
3. **匹配页支持手动切换**：星座(`constellation`)与生肖(`zodiac`)页双方除生日/年份推算外，新增「切换：XX」手动选择器（`picker mode=selector`），直接选星座/生肖，覆盖推算结果；手动选择优先级最高（compute 用 `zA || derive()`）。
4. **抽签/运势/转盘加声音开关**：draw/fortune/wheel 三页新增右上角 `.sound-fab` 悬浮按钮（ic-sound-on/off），调用 `utils/sound` 的 `setMuted/isMuted`，与木鱼页一致；全局样式加 `.sound-fab`。
5. **8 个工具图标重做**：app.wxss 中 `ic-wheel`(转盘带指针)/`ic-fortune`(太阳光芒)/`ic-draw`(签筒)/`ic-constellation`(连星)/`ic-zodiac`(爪印)/`ic-memo`(记事本)/`ic-countdown`(沙漏)/`ic-woodfish`(小鱼) 全部换成更易区分、风格统一的线性 SVG。
- 校验：18 个 JS、14 个 JSON 全过；10 页面四件套齐全；总大小 ~247KB（含 wav+svg，远低于 2MB）。

## 第六轮：我的页 2 项修改（同日追加）
用户反馈后修改（pages/about/about）：
1. **头像可上传**：资料头头像由静态 SVG 改为可点击（bindtap="chooseAvatar"），用 `wx.chooseMedia`（album/camera）选图，经 `wx.getFileSystemManager().saveFile` 持久化到 `wx.env.USER_DATA_PATH` 并存 `qw_avatar`（同时删除旧头像文件）；有头像时显示 `<image mode=aspectFill>`，无则回退占位 SVG；头像右下角加「换头像」徽标。未选时仍显示默认人形图标。
2. **移除「数据与隐私」卡片**：整个「数据与隐私」卡片（数据存储位置/上传服务器/清除全部本地数据按钮）全部删除，用户看不到该标题及以下文字内容；同步移除 about.js 的 onClear 与 util 的 clearAll 引用（该页逻辑已无外部依赖）。
- 同步更新 preview.html 的「我的」屏（头像加换头像徽标、去掉数据与隐私卡）。
- 校验：about.js `node --check` 通过；wxml 中已无「数据与隐私」字样；chooseAvatar 已接线。

## 第七轮：木鱼/对话框/匹配页/声音按钮/我的头 7 项修改（同日追加）
用户反馈后修改：
1. **木鱼按参考图重绘**：`/images/woodfish.svg` 改为经典木鱼剪影（纯白填充、圆润鱼身、底部小凸柄、粗曲线敲击缝），黑底白鱼，去掉眼睛与渐变，更贴近参考图。
2. **对话框文字完整可见**：`app.wxss` 为 `input.field` 增加显式 `height:88rpx` / `line-height:44rpx`，确保 placeholder 与输入文字不被截断；逐个检查所有底部弹层输入框。
3. **备忘录标题与内容分开**：`pages/memo/memo.wxml` 中标题与内容 textarea 分别包进 `.field-card` 卡片并加 `.field-label` 标签，视觉上成为两个独立输入框，不再粘连；`pages/wheel/wheel.wxml` 的「新增模板」弹层同样套用该模式保持统一。
4. **生肖匹配只留生肖切换**：`pages/zodiac/zodiac.wxml/.js` 删除年份 date picker 与 yA/yB/dateA/dateB，只保留 `picker mode=selector` 直接选生肖；默认「龙 vs 鼠」。
5. **星座匹配只留星座切换**：`pages/constellation/constellation.wxml/.js` 删除日期 picker 与 rangeA/rangeB，只保留星座选择器；默认「双子座 vs 天蝎座」。
6. **声音开关按钮移到右下角**：`app.wxss` 的 `.sound-fab` 从 `top:22rpx` 改为 `bottom: calc(40rpx + env(safe-area-inset-bottom))`，fixed 在右下角，不遮挡标题和主要卡片。
7. **我的页资料头优化**：去掉右上角「编辑」二字；顶部昵称下方同时显示性别与生日（`保密 · 还未设置生日`），删除 `.edit-hint` 样式。
## 第八轮：木鱼真实造型 + 输入框文字居中（同日追加）
用户反馈后修改：
1. **按真实木鱼重绘 SVG**：搜索真实木鱼（团鱼形佛教法器）特征后，重绘 `/images/woodfish.svg`：圆润团鱼形主体、腹部中空、头部正中开口（出声缝）、背部斜坡敲击线，去掉卡通鱼眼/鱼尾，更贴近真实法器造型。
2. **输入框文字垂直居中**：`app.wxss` 调整 `input.field`：去掉上下 padding，改为 `height:88rpx; line-height:88rpx; padding:0 24rpx; box-sizing:border-box;`，并加 `.field-card input.field { padding:0; }`；解决倒数/备忘录/转盘弹层中 placeholder 与输入文字偏上的问题。
- 校验：10 个页面 JS 全过、14 个 JSON 全过、10 页面四件套齐全；总大小约 412KB（远低于 2MB）。

## 第九轮：声音开关按钮改为随页面滚动、各页同位置
用户要求：声音开关要放在每个页面同一个位置，且随页面上下滚动而移动（之前是 `position:fixed` 固定在屏幕右下角，不随页面滚）。
- 改动：`app.wxss` 删除 `.sound-fab { position:fixed; bottom:... }`，改为页面内顶部右上角控制条 `.sound-bar`（display:flex; justify-content:flex-end），`.sound-fab` 改 `position:relative`（回到文档流，随页面滚动）。draw/fortune/wheel 三页 wxml 中把 `<view class="sound-fab">` 包进 `<view class="sound-bar">`。
- 类名避坑：原拟用 `.topbar` 但 countdown/memo 页面已用 `.topbar`（页面标题栏），故改用专用类 `.sound-bar` 避免污染。
- 木鱼页（woodfish）的静音按钮本就在底部 `.ctrls` 文档流内（toggleMute），随页面滚动，无需改。
- 校验：三页 sound-bar 已包裹 sound-fab，无残留 position:fixed（仅 .mask/.sheet 弹层保留 fixed，正确）。

## 第十轮：5 项修改（同日追加）
用户反馈后修改：
1. **我的页箭头同行**：`pages/about/about.wxml` 把性别/生日 picker 中的箭头 `›` 包进 `.picker-val` 内部，并在 `about.wxss` 把 `.picker-val` 改为 `display:inline-flex; align-items:center`、箭头字号降到 32rpx，确保箭头紧贴文字右侧、垂直居中，不再掉到下方。
2. **备忘录自定义分类并精简**：`pages/memo/memo.wxml` 把分类由固定 chips 改为可输入的 `.field-card` 输入框（placeholder「输入分类，如：工作 / 生活」），下方保留 5 个快捷 chips；**移除「标签」和「提醒日期」两个区块**。`pages/memo/memo.js` 同步删除 `tagsText`、`remindDate`、`onRemind`，`save` 只保存 title/content/category；搜索与列表展示也去掉了标签/提醒日期。
3. **星座/生肖匹配头像用图标**：不再用文字做头像。在 `app.wxss` 新增 12 个生肖 SVG（`.zodiac-0`~`.zodiac-11`）和 12 个星座符号 SVG（`.cons-0`~`.cons-11`）。`pages/zodiac/zodiac.wxml` 与 `pages/constellation/constellation.wxml` 把头像 `<view class="avatar">` 改为 `<view class="ic zodiac-icon zodiac-{{idxA}}">` / `<view class="ic cons-icon cons-{{idxA}}">`；并删除对应 `.avatar` 的 `font-size` 样式。
4. **运势/抽签声音按钮放底部**：`pages/fortune/fortune.wxml` 和 `pages/draw/draw.wxml` 把顶部 `.sound-bar` 移到页面内容底部（仍随页面滚动），并加 `.bottom-bar` 样式控制间距；`wheel` 页声音开关仍留在顶部右上角。
5. **转盘输入框 placeholder 简化**：`pages/wheel/wheel.wxml` 中选项添加输入框的 placeholder 由「输入新选项，回车添加」简化为「输入新选项」。
- 校验：全部修改后 10 页面 JS `node --check` 通过、14 个 JSON 合法、10 页面四件套齐全；总大小约 420KB（远低于 2MB）。
- 补充：memo 顶部分类筛选 chips 现在会根据已有备忘录自动收集自定义分类，与固定分类一起展示；搜索 placeholder 也同步改为「搜索标题 / 内容」。
